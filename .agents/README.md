# Portable agent assets for this repository.

Feed Lens first-version work should assume Gemini-only LLM support.

- Use `GEMINI_API_KEY` as the canonical local testing key name.
- Treat `.env` and key values as sensitive.
- Do not add multi-provider workflows unless explicitly requested.
