# Place Codex-specific environment/config files here when needed.

For local FeedLens testing, the repository may use a root `.env` file with `GEMINI_API_KEY`.

Do not commit `.env`, print key values, or bake `GEMINI_API_KEY` into distributable extension assets.
